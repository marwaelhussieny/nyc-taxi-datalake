connector.name=iceberg
iceberg.catalog.type=glue
hive.metastore.glue.region=us-east-1
fs.native-s3.enabled=true
s3.region=us-east-1
# Credentials are picked up from the standard AWS credential chain via
# environment variables passed into the container (see docker-compose.yml) -
# never hardcoded here.
