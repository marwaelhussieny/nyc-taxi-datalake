# Core pipeline dependencies (Dagster is installed separately inside its
# own Docker container per docker-compose.yml, not needed locally to run
# bronze/silver/gold scripts directly)
pyiceberg[glue,pyarrow]>=0.11.0
pyarrow>=16.0
pandas>=2.2
boto3>=1.34
requests>=2.31
pytest>=8.0
